export default interface HeaderPropsType {
    color: string;
    showLoginButton?: boolean;
    showSignupButton?: boolean;
    loginColor:string;
    singupColor:string;
    imgUrl: string;
    text?: string;
    name?: string;
}